import { HiddenListViewDirective } from './hidden-list-view.directive';

describe('HiddenListViewDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenListViewDirective();
    expect(directive).toBeTruthy();
  });
});
