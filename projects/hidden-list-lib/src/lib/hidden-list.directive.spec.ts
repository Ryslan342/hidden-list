import { HiddenListDirective } from './hidden-list.directive';

describe('HiddenListDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenListDirective();
    expect(directive).toBeTruthy();
  });
});
